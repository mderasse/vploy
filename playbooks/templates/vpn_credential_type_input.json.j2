{
 "fields": [
{% for key, value in password.vpn.iteritems() %}
  {
   "id": "vpn_{{ key }}_password",
   "type": "string",
   "label": "Password for VPN user {{ key }}",
   "secret": true,
   "help_text": "",
   "multiline": false
  }
{%- if not loop.last -%}
,
{% endif %}
{% endfor %}
 ],
 "required": [
{% for key, value in password.vpn.iteritems() %}
  "vpn_{{ key }}_password"
{%- if not loop.last -%}
,
{% endif %}
{% endfor %}
 ]
}